services:
  - type: web
    name: Kardex_Vacaciones_Web
    runtime: python
    pythonVersion: 3.11.9
    buildCommand: "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"